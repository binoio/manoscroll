# ship-it configuration for ManoScrollApp
APP_NAME="ManoScrollApp"
PROJECT_TYPE="spm"
BUILD_DIR="build"
DIST_DIR="dist"
CONFIGURATION="Release"

# Custom hooks for ManoScrollApp
POST_BUILD_HOOK="bash Scripts/bundle.sh"
